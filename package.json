{
  "name": "hello-sweep",
  "version": "1.0.0",
  "description": "Sweep SDK(sweepjs) sample application",
  "scripts": {
    "start": "run-p start:server start:visualizer",
    "start:server": "cd ./server && npm start",
    "start:visualizer": "cd ./visualizer && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "webpack",
    "typescript",
    "three.js"
  ],
  "author": "e_ntyo",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.0.2"
  }
}
