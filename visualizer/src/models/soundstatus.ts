export class SoundStatus {
  frequency: number;
  distance:  number;

  constructor(
    frequency: number,
    distance:  number
  ) {
    this.frequency = frequency;
    this.distance  = distance;
  }
}